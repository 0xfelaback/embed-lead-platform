## where AI helped, where it was wrong, what I changed

| Section | Where AI helped | Where AI was wrong | What changed |
|---------|----------------|-------------------|--------------|
| Widget Management API | 1. Circular import resolution: Fixed SQLAlchemy circular import issues in `src/module/schemas` by ensuring TYPE_CHECKING usage, updating `__init__.py` import order (Tenant → Widget → Submission), and updating imports to use centralized pattern. Result: InvalidRequestError and ImportError resolved.<br><br>2. Domain whitelist security fix: Changed from empty list allowing all domains to empty list allowing no domains (secure by default). Updated descriptions and changed from Optional to List with default_factory.<br><br>3. Custom exception status codes: Fixed 500 errors by adding ResourceAccessDeniedError to re-raise lists in orchestrator's delete/update workflows and router exception handling.<br><br>4. Soft-delete implementation: Fixed schema issue where deleted_at had default lambda - changed to None so it only gets set on soft-delete.<br><br>5. Updated_at timestamp: Added to repository update method whenever fields are modified.<br><br>6. Soft-delete business logic: Added is_deleted=False filters to get_by_id (404 for deleted), get_by_tenant_id (excludes from list), belongs_to_tenant (prevents unauthorized access), and update (inherits via get_by_id).<br><br>7. Widget creation defaults: Explicitly set is_deleted=False and deleted_at=None in repository create method. | 1. Initial diagnosis of exception handling: Initially thought the global exception handler in main.py was the issue for 500 errors. The handler was actually correct - the problem was in the orchestrator layer catching and converting exceptions. Corrected after investigating the orchestrator code. | 1. I corrected the domain_whitelist behavior from "empty = allow all" to "empty = allow no domains" for security.<br><br>2. I applied several fixes to DTOs and orchestrators (UUID types, model_dump mode="json", pagination structure, exception types) that I did not implement.<br><br>3. I removed the comment "Build base query - always filter out deleted widgets" from repository, keeping it simpler. |
| Fast, cached widget delivery & Public submission endpoint | 1. Submission module implementation: Created complete submission infrastructure including repository (already existed), service layer with business logic, orchestrator workflow, and router endpoint.<br><br>2. DTO completion: Fixed incomplete SubmissionResponse DTO by adding all required fields (id, widget_id, tenant_id, payload, client_ip, geo_data, user_agent, status, created_at) to match the orchestrator usage and schema.<br><br>3. Router endpoint fix: Fixed submission ingestion endpoint in widget router to use proper workflow, extract client information (IP, user agent), implement payload size validation (100KB limit), and use correct field names (form_data instead of payload).<br><br>4. Import corrections: Fixed typo in router imports (SunmissionRequest → SubmissionRequest) and removed duplicate submission DTOs from widget.py, using centralized submission.py instead.<br><br>5. Service layer integration: Connected submission service to validate widget exists and is active before creating submission, ensuring proper business logic flow.<br><br>6. Status handling: Integrated SubmissionStatus enum throughout the stack (schema, service, DTO) with proper SUCCESS/FAILED states. | 1. DTO field mismatch: Initially created incomplete SubmissionResponse DTO that didn't match the orchestrator's expectations. I pointed out the DTO was incomplete and I had to add all the missing fields to match the full submission entity structure.<br><br>2. Field name confusion: Initially used payload field name in DTO but the request uses form_data. Had to align these properly and update the router to use the correct field names. | 1. I updated the SubmissionRequest DTO to use form_data instead of payload and added the honeypot field for spam protection.<br><br>2. I simplified the SubmissionResponse DTO structure initially, then asked me to complete it with all necessary fields.<br><br>3. I removed the test_submission.py file I created, indicating they'll handle testing differently. |
