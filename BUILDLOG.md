## where AI helped, where it was wrong, what I changed

| Section | Where AI helped | Where AI was wrong | What changed |
|---------|----------------|-------------------|--------------|
| Widget Management API | 1. Circular import resolution: Fixed SQLAlchemy circular import issues in `src/module/schemas` by ensuring TYPE_CHECKING usage, updating `__init__.py` import order (Tenant → Widget → Submission), and updating imports to use centralized pattern. Result: InvalidRequestError and ImportError resolved.<br><br>2. Domain whitelist security fix: Changed from empty list allowing all domains to empty list allowing no domains (secure by default). Updated descriptions and changed from Optional to List with default_factory.<br><br>3. Custom exception status codes: Fixed 500 errors by adding ResourceAccessDeniedError to re-raise lists in orchestrator's delete/update workflows and router exception handling.<br><br>4. Soft-delete implementation: Fixed schema issue where deleted_at had default lambda - changed to None so it only gets set on soft-delete.<br><br>5. Updated_at timestamp: Added to repository update method whenever fields are modified.<br><br>6. Soft-delete business logic: Added is_deleted=False filters to get_by_id (404 for deleted), get_by_tenant_id (excludes from list), belongs_to_tenant (prevents unauthorized access), and update (inherits via get_by_id).<br><br>7. Widget creation defaults: Explicitly set is_deleted=False and deleted_at=None in repository create method. | 1. Initial diagnosis of exception handling: Initially thought the global exception handler in main.py was the issue for 500 errors. The handler was actually correct - the problem was in the orchestrator layer catching and converting exceptions. Corrected after investigating the orchestrator code. | 1. User corrected the domain_whitelist behavior from "empty = allow all" to "empty = allow no domains" for security.<br><br>2. User applied several fixes to DTOs and orchestrators (UUID types, model_dump mode="json", pagination structure, exception types) that I did not implement.<br><br>3. User removed the comment "Build base query - always filter out deleted widgets" from repository, keeping it simpler. |
